# CDF Compare Tool — 단계별 바이브 코딩 프롬프트 (최종본)

Rev.0.027 기준. Claude Code / Cursor / Windsurf 어디서든 그대로 붙여넣어 쓸 수 있습니다.
줄번호는 **실제 소스 파일을 열어 확인한 값**입니다 (`cdf_compare_web.py` 7,345줄 / `cdf_compare_tool.py` 1,140줄).

---

## 사용법

1. **`CLAUDE.md` 를 프로젝트 루트에 저장합니다** — 별도 파일로 전달드렸습니다.
   `cdf_compare_web.py` 와 같은 폴더에 두세요. Cursor 는 `.cursorrules` 로 저장.
   저장 전에 §8 빌드 명령어만 채워주세요.
2. **`NOTES.md` 도 같은 폴더에** 두세요. 나중에 할 일 목록입니다.
3. **Git 브랜치를 파고 시작합니다.** `git init` → 첫 커밋 → `git checkout -b perf/columnar`
4. **한 단계씩** 붙여넣고, 완료 기준을 통과한 뒤 커밋하고 다음으로 갑니다.

> ⚠️ **A·B단계를 먼저 끝내야 합니다.** 그 전에는 툴이 실제 폴더를 인식하지 못하고
> Pass 분석이 통째로 비어 나옵니다. 그 상태로 회귀 스냅샷을 찍으면
> **"비어 있어야 통과"** 가 되어 검증이 거꾸로 작동합니다.

---

## 전체 순서

| | 단계 | 성격 | 효과 |
|---|---|---|---|
| **A** | Reliability Type 제거 (폴더 4토막) | 데이터 탐색 | 폴더 인식 복구 |
| **B** | Pre↔Post 조인을 DEVICE_ID 로 | **판정 변경** | Pass 분석 복구 |
| **0** | 회귀 검증 하네스 | 안전장치 | — |
| 1 | payload details 제거 | 성능 | JSON 18배, 크기 33배 |
| 2 | 파싱 캐시 + 축출 수정 | 성능 | 재분석 43배 |
| 3 | **컬럼형 파서** | 성능 | 파싱 16배, 메모리 37배 |
| 4 | 요약 먼저 push | 체감 | 대기 체감 최소 |
| 5 | ProcessPool | 성능 | Total 분석 코어 수만큼 |
| 6 | 프론트 최적화 | 체감 | 조작감 |

**A → B → 0 → 1~3** 까지가 투자 대비 효과가 가장 큽니다.

---

## §A. Reliability Type 제거 (폴더명 4토막)

```text
프로젝트 루트의 CLAUDE.md 를 읽고 시작해줘.

조건 폴더 명명 규칙에서 Reliability Type 을 완전히 제거해줘.

## 배경
parse_data_folder_name() 은 폴더명이 언더바 4개(5토막)여야 인식한다:
  순번_Ver_ReliabilityType_Lot_Purpose
실제 사내 규칙은 언더바 3개(4토막)다:
  순번_Ver_Lot_Purpose        예) 00_MVT0-0_1111_111
그래서 실제 폴더가 파싱에 실패해 Reliability Type 드롭다운이 비고,
그다음 단계로 진행이 안 된다.

Reliability Type 은 불필요한 필드로 확정됐다. 드롭다운째로 삭제한다.
Reliability Items(고정 9종 RELIABILITY_ITEMS)는 그대로 유지한다. 절대 건드리지 마라.

## 백엔드 (cdf_compare_web.py)

1. parse_data_folder_name (6368줄)
   - split("_", 4) → split("_", 3),  len(parts) != 5 → != 4
   - _, ver, rel_type, lot, purpose = parts  →  _, ver, lot, purpose = parts
   - 반환 dict 에서 "type" 키 삭제
   - sequence.isdigit() 검사와 strip 처리는 그대로 유지

2. device_data_folders (6385줄)
   - rel_type 파라미터 삭제, 6393줄의 rel_type 필터 조건 삭제

3. selected_data_path (6406줄 부근)
   - rel_type 추출(6406) / 필수값 검사(6409) / 호출 인자(6412) 에서 type 제거

4. lookup_options (6553줄 부근)
   - 기본 field 를 "type" → "ver" 로 변경 (6553줄)
   - field == "type" 분기(6562~6563) 삭제
   - rel_type 추출과 빈 값 조기 반환(6565~6566) 삭제
   - device_data_folders 호출에서 rel_type 인자 제거 (6569, 6575, 6581, 6584)

5. LOOKUP_FIELDS (6295줄) 에서 "type" 제거

6. has_base_analysis_selection (5408줄)
   - required 를 ["device", "ver", "purpose", "lot"] 로

7. cache_key_for (6000줄 부근)
   - fields 에서 "type" 항목 삭제
   - CACHE_SCHEMA 를 17 → 18 로 올려라 (기존 캐시 무효화 목적)

## 프론트엔드 (HTML 문자열 상수 내 — 이 작업에 한해 수정 허용)
55~4664줄이 하나의 문자열이다. 따옴표가 깨지지 않게 주의해라.

8. 1851줄의 Reliability Type 필드 div 한 줄 삭제
   <div class="field"><label>Reliability Type</label><select id="typeSelect"></select></div>

9. lookupOrder (2006줄) 에서 ["type", "typeSelect"] 항목 삭제

10. lookupState (2015줄) 에서 type: "" 삭제

11. hasBaseSelection 배열 (2018줄) 에서 "type" 삭제

12. type / typeSelect 를 참조하는 JS 가 더 있는지 grep 으로 전부 확인하고 정리해라.
    단 spec_out_type / detailSpecOutType 는 전혀 다른 것이므로 건드리지 마라.

## 기존 tools/ 가 있다면
tools/_regression_lib.py 의 SELECTION_KEYS 등에서도 "type" 을 제거해라.

## 완료 기준 (반드시 브라우저로 눈으로 확인)
- python cdf_compare_web.py 실행 후 브라우저에서
  Device 에 SM3502Q 입력 → Ver.(MVT0-0) → Lot No.(1111) → Purpose(111) 가 채워진다
- Reliability Items 에서 HTOL 선택 → Read-out(1000hrs) → FT Temp.(Room) 까지 채워진다
- 화면에 Reliability Type 칸이 보이지 않는다
- 폴더 00_MVT0-0_1111_111 을 이름 변경 없이 인식한다
```

---

## §B. Pre↔Post 조인을 DEVICE_ID 기준으로

```text
프로젝트 루트의 CLAUDE.md 를 읽고 시작해줘. §3-3 이 이 작업의 배경이다.

Pre↔Post 유닛 매칭 기준을 Serial # 에서 DEVICE_ID 로 바꿔줘.

## 배경 (실측 확인)
현재 코드는 record['sample'](= Serial # 컬럼)으로 Pre 와 Post 를 조인한다.
그런데 실제 데이터는 Serial # 체계가 서로 다르다.

  PRE  Serial 예시 : 100041, 100049, 100057 ...  (로트 전체 3,173 유닛, 6자리)
  POST Serial 예시 : 1, 2, 3, 4 ...              (시험 투입 79 유닛, 1번부터 재부여)
  → Serial # 교집합 0건

그 결과 Pass 분석이 통째로 비어 있다(results=[], items={}).
DEVICE_ID 로 매칭하면 Post 78개가 Pre 에 전부 붙는다.

DEVICE_ID 는 별도 메타 컬럼이 아니라 **Test Item 컬럼 중 하나**다.
  - Test Name 행의 값이 정확히 "DEVICE_ID"
  - 이 파일에서는 129번째 열, Test Number 8.49, Units "Binnary"
  - 컬럼 위치는 파일마다 다를 수 있으므로 **반드시 이름으로 찾아야 한다**
  - 모든 시험 항목 파일에 같은 이름으로 들어간다 (사용자 확인 완료)

## 할 일

1. cdf_compare_tool.py 의 extract_item_records_from_meta_table 에서
   Test Name 이 "DEVICE_ID"(대소문자/공백 무시)인 컬럼을 찾아, 그 값을 각 레코드에
   record["device_id"] 로 넣어라.
   - 상수로 빼라: ID_ITEM_NAMES = ("DEVICE_ID",)   ← 나중에 이름이 늘 수 있음
   - 기존 record["sample"](Serial #)은 **그대로 유지**한다. 화면 표시는 계속 Serial # 다.
   - DEVICE_ID 는 to_float 로 숫자화하지 말고 **문자열 원본**을 보존해라
     (앞자리 0 손실 방지). 값이 없으면 "" 로 둔다.
   - legacy 경로(extract_item_records_from_legacy_table)에는 DEVICE_ID 가 없으므로
     device_id 는 "" 로 둔다.

2. DEVICE_ID 컬럼 자체는 **분석 대상 항목에서 제외**해라.
   측정값이 아니라 식별자다. 지금은 Units="Binnary" 라 단위 필터에 우연히 걸리는데,
   그 우연에 의존하지 말고 명시적으로 제외할 것.

3. cdf_compare_web.py 의 조인 키를 바꿔라.
   - record_sample_id() / records_by_sample() / item_analysis() 의 Pre 조인부에서
     device_id 가 **양쪽 모두 비어있지 않으면 device_id 로** 조인한다.
   - Post 레코드의 device_id 가 비어 있으면 → Pre 매칭 없음 →
     기존과 동일하게 result="NO PRE SAMPLE" 로 처리한다.
     **절대 다른 유닛에 붙이지 마라.**
     (실측: Post Serial 79번이 4행 전부 DEVICE_ID 빈칸, Bin 4)
   - Pre 에 같은 device_id 가 여러 번 있으면 (실측 11건)
     **마지막 등장 레코드**를 채택한다. filter_records_to_last_sample 과 같은 규칙이다.
   - 어느 한쪽 파일에 DEVICE_ID 컬럼이 아예 없으면 기존처럼 sample(Serial #) 로
     폴백하고, payload 에 경고를 남겨라.

4. 매칭 결과를 payload 에 노출해라. 조용히 실패하면 안 된다.
   payload["match_summary"] = {
       "key": "device_id" | "sample",
       "post_units": N, "matched": N, "unmatched": N
   }
   analyze_to_json 의 message 에도 "Pre 매칭 141/145" 같은 형태로 한 줄 넣어라.

5. 재시험 처리는 이번에 건드리지 마라.
   Serial # 와 DEVICE_ID 가 1:1 이라 filter_records_to_last_sample 결과는 동일하다.
   NOTES.md 의 P0(재시험 이력) 작업은 별도로 진행한다.

## 완료 기준
- Pass 분석 시 results 가 비어 있지 않다 (기대: 단위 필터 통과 462개 항목 수준)
- payload["match_summary"] 가 matched 141 / unmatched 4 로 나온다 (145행 기준)
- DEVICE_ID 가 빈 유닛(Serial 79)이 NO PRE SAMPLE 로 처리되고,
  다른 유닛의 Pre 값이 잘못 붙지 않는다
- Fail 분석 결과가 이전과 동일하다 (Fail Items 5, Fail Samples 22)
- 결과 표에 표시되는 Sample No. 는 여전히 Serial # 다
```

---

## §0. 회귀 검증 하네스

리팩터링 전에 **"숫자가 안 바뀌었다"를 기계로 증명할 수단**부터 만듭니다.

> 이미 `tools/` 가 있다면 새로 만들지 말고 **수정**하라고 지시하세요.
> 단 `tests/golden/` 은 **반드시 지우고 다시 만들어야 합니다** (A·B단계로 결과가 바뀝니다).

```text
프로젝트 루트의 CLAUDE.md 를 읽고 시작해줘. §5-3 을 특히 주의해서 지켜라.

성능 리팩터링을 시작하기 전에, 결과가 바뀌지 않았음을 자동으로 검증할 회귀 테스트
하네스를 만들어줘. (tools/ 가 이미 있으면 새로 만들지 말고 고쳐라)

## 절대 지켜야 할 것 — 지난번에 실패한 부분이다
- 데이터를 임시 폴더나 스크래치패드로 **복사하지 마라**. 사용자가 알려준 실제 경로에서
  그대로 읽어라.
- 파서를 통과시키려고 **폴더명이나 파일명을 바꾸지 마라.**
- 합성 픽스처로 대체하지 마라.
- **분석 결과가 비어 있으면 그것을 정답으로 저장하지 마라.** 멈추고 원인을 보고해라.
  (results 가 빈 배열이면 그 자체가 버그 신호다)
- 경로나 데이터에서 막히면 **멈추고 물어봐라.** 스스로 우회하지 마라.

## 목표
지금의 분석 결과를 "정답 스냅샷"으로 저장해 두고, 이후 어떤 리팩터링을 하더라도
동일 입력 → 동일 출력임을 한 번의 명령으로 확인할 수 있게 한다.

## 만들 것

### 1) tools/regression_snapshot.py
- 입력: --data-root <경로>  --out tests/golden/  [--limit N]
- cdf_compare_web.py 의 analyze_to_json() 과 analyze_fail_to_json() 을 직접 호출한다
  (HTTP 서버를 띄우지 말 것)
- 각 (조건, 모드) 조합마다 저장:
  * results 전 행 — item, test_number, unit, lower_limit, upper_limit,
    mea_s, diff_s, result
  * selected_summary 전 행
  * over_sigma
  * match_summary
  * items 중 SELECT 된 항목의 details 전부
- float 은 repr 그대로 저장한다. 반올림 금지 (미세한 차이도 잡아야 함)
- **results 가 비어 있는 조합이 있으면 저장하지 말고 에러로 종료**하고 원인을 출력해라

### 2) tools/regression_check.py
- 입력: --data-root <경로>  --golden tests/golden/
- 비교 규칙:
  * 문자열/정수: 완전 일치
  * float: 절대오차 1e-9 또는 상대오차 1e-12 이내면 통과
  * None ↔ 값 은 불일치
  * 항목 집합, 항목 순서, details 행 수, 샘플 순서까지 전부 비교
- 출력: 통과/실패 요약 + 실패 시 처음 20건의 (조합, 항목, 필드, 이전값, 현재값)
- 실패하면 exit code 1

### 3) tools/bench.py
- read_table / extract_item_records / filter_records_to_last_sample /
  calculate_results_vectorized / payload_with_items / json.dumps / 캐시 쓰기
  각 구간의 초 단위 시간과 전체 대비 비중
- payload 의 JSON 바이트 크기, 항목 수, 총 detail 행 수
- --repeat 기본 3, 중앙값 사용

## 주의
- 기존 소스(cdf_compare_web.py / cdf_compare_tool.py)는 **한 줄도 수정하지 마라.**
- 서버 전역(CURRENT_APP, CURRENT_PAYLOADS 등)에 의존하지 말고 함수를 직접 호출해라.
- 완료 후 tools/README.md 에 실행 예시 3줄을 적어줘.
```

**완료 기준**
- 스냅샷 생성이 정상 종료하고, **pass 스냅샷이 비어 있지 않다**
- 코드를 안 바꾼 상태에서 `regression_check` 가 **PASS**
- `bench.py` 출력을 저장해 둔다 (이후 단계마다 비교)

---

## §1. payload 에서 전 항목 details 제거

측정 결과 이 단계 하나로 **JSON 직렬화 12.5초 → 0.7초, payload 839MB → 25MB**.

```text
프로젝트 루트의 CLAUDE.md 를 읽고 시작해줘.

분석 결과 payload 가 불필요하게 거대해지는 문제를 고쳐줘.

## 현재 문제 (측정 결과)
항목 1,000 × 유닛 3,000 규모에서:
- payload JSON 이 839 MB, json.dumps 만 12.5초
- 이 덩어리가 디스크 캐시 + CURRENT_PAYLOADS + JOBS 세 곳에 동시 상주

원인: payload_with_items() (6280줄) 가 payload['results'] **전체**를 순회하면서
모든 항목에 대해 item_to_json() 을 호출한다. SELECT 되지 않은 항목까지 샘플별
details 를 전부 만든다.

그런데 UI 는 선택된 항목 1개의 상세만 표시하고, 이미 다음이 갖춰져 있다:
- GET /item?name=&mode= 엔드포인트 (do_GET 7018줄, /item 분기 7054줄)
- split cache 의 항목별 파일 저장 (save_cached_analysis / load_cached_item)
지연 로딩 인프라가 이미 있는데 안 쓰고 있다.

## 할 일

1. payload_with_items() 수정
   - payload['selected_summary'] 에 있는 항목(= SELECT 된 것)에 대해서만
     item_to_json() 을 호출한다
   - selected_summary 가 비어 있으면 items 는 빈 dict
   - payload['items_are_partial'] = True 를 넣어 "상세는 지연 로딩" 임을 명시

2. GET /item 핸들러가 items 에 없는 항목을 요청받았을 때 정상 동작하는지 확인하고,
   안 되면 고쳐라.
   - 현재 폴백 순서: CURRENT_PAYLOADS[mode]['items'] → CURRENT_ITEMS →
     load_cached_item() → item_to_json(CURRENT_APP, name)
   - 마지막 폴백이 살아 있어야 이 변경이 안전하다.
     CURRENT_APP 이 None 이면 실패하므로(fail 잡이 None 을 넣는다),
     그 경우 캐시에서 복구하거나 명확한 에러 메시지를 반환하게 해라.
   - 새로 만든 item 은 CURRENT_PAYLOADS[mode]['items'] 에 캐싱해 재요청을 막아라.

3. GET /progress (7083줄 분기) 가 완료 시 payload 전체를 반환하는 것을 멈춰라.
   - {status, progress, message, job_id} 만 반환
   - 프런트가 어차피 /latest-analysis 로 다시 받으므로 같은 덩어리를 두 번 전송 중이다
   - JOBS[job_id] 에도 payload 를 저장하지 마라 (메모리 누적 방지)
   - 프런트 JS 에서 /progress 응답의 payload 를 쓰는 곳이 있는지 grep 으로 확인하고,
     있으면 /latest-analysis 를 쓰도록 최소 수정 (이 경우에 한해 HTML 문자열 수정 허용)

4. save_cached_analysis() 는 전 항목 상세를 캐시에 저장해도 된다.
   **캐시에는 전 항목 상세가 있어야 /item 지연 로딩이 동작한다.**
   단 메모리에 한꺼번에 올리지 말고 **한 항목씩 만들어 파일로 쓰고 버려라.**

## 완료 기준
- regression_check PASS
- bench.py 에서 json.dumps 시간과 payload 크기가 10배 이상 감소
- UI 에서 항목 클릭 시 상세 표와 차트가 이전과 동일하게 뜬다 (수동 확인)
```

---

## §2. 파싱 결과 디스크 캐시 + 캐시 축출 버그

재분석·Total 재실행이 **43배** 빨라집니다.

```text
프로젝트 루트의 CLAUDE.md 를 읽고 시작해줘.

파일 파싱 결과를 재사용하지 못하는 문제 두 가지를 고쳐줘.

## 문제 A — 같은 파일을 반복 파싱 (캐시 축출 버그)
cached_item_records() (4793줄) 는 결과를 저장할 때
"같은 abspath 이지만 key 가 다른 항목"을 모두 삭제한다 (4806~4807줄).

그런데 두 분석 모드가 같은 Post 파일을 서로 다른 플래그로 요청한다:
  - Pass 경로 make_app()           : cached_item_records(post_path, last_sample=True)
  - Fail 경로 read_post_stage_rows() : cached_item_records(path) → last_sample=False

프런트엔드는 pass 와 fail 을 Promise.all 로 **동시에** 실행하므로,
두 잡이 서로의 캐시 항목을 계속 밀어내면서 같은 파일을 반복 파싱한다.

### 고칠 방향
캐시를 2계층으로 나눈다.
  - RAW 캐시: key = (abspath, st_mtime_ns, st_size) → extract_item_records() 원본
  - last_sample 필터는 RAW 에서 파생시키는 값싼 후처리로 분리
축출 로직은 "같은 abspath 의 **오래된 mtime/size** 항목"만 지우도록 바꾼다.
last_sample 만 다른 형제를 지우면 안 된다.

## 문제 B — 파싱 결과가 재시작·조건 변경 시 사라짐
현재 분석 **결과** 캐시(.analysis_cache)는 있지만 파싱 **중간 결과** 캐시가 없다.
조건을 조금만 바꾸거나 툴을 재시작하면 26MB CSV 를 처음부터 다시 읽는다.

### 고칠 방향
  - 위치: 기존 CACHE_DIR 아래 parse/ 서브디렉터리
  - 키: sha256(abspath | st_mtime_ns | st_size | PARSE_CACHE_VERSION)
  - 포맷: 값 배열은 numpy(.npz), 메타는 pickle 로 npz 안에
    (외부 패키지 추가 금지 — numpy 와 표준 라이브러리만)
  - tmp 에 쓴 뒤 os.replace 로 원자적 교체 (기존 write_json_file 과 같은 패턴)
  - 로드/저장 실패는 조용히 무시하고 정상 파싱으로 폴백 (캐시는 최적화일 뿐)
  - 캐시 총량이 일정 크기(예: 2GB)를 넘으면 오래된 것부터 지우는 정리 함수 추가

## 추가 (같은 헬퍼로 함께)
cache_key_for() (5991줄) 의 분석 결과 캐시 키에 파일 mtime/size 가 없어서,
같은 파일명으로 CSV 를 수정하면 낡은 결과가 반환된다.
위에서 만든 파일 스탬프 헬퍼를 재사용해 'pre_stamp' / 'post_stamps' 를 추가하고,
pass 모드도 post_files 전체 세트를 반영하게 해라 (현재는 post_files[-1] 만).

## 완료 기준
- regression_check PASS
- 같은 조건을 두 번 연속 분석하면 두 번째가 확연히 빠르다 (bench.py 로 확인)
- CSV 를 하나 수정(타임스탬프 변경)한 뒤 재분석하면 결과가 갱신된다
- pass 와 fail 을 동시에 실행해도 같은 파일을 두 번 파싱하지 않는다
  (진입 시 임시 로그로 확인하고, 확인 후 제거)
```

---

## §3. 컬럼형 파서 도입 (핵심 단계)

파싱 **16배**, 메모리 **37배**.

```text
프로젝트 루트의 CLAUDE.md 를 읽고 시작해줘. §7 이 이 작업의 근거다.

데이터로그 파싱을 record-dict 구조에서 컬럼형(numpy) 구조로 전환해줘.

## 배경 (실측)
항목 1,000 × 유닛 3,000 파일 하나를 파싱할 때:
  - extract_item_records() 가 전체 시간의 69% (약 10초)
  - 셀 1개당 row_value_at 7회, cell_text 5회, to_float 2회 호출
  - 측정값 1개당 dict 1개 → 300만 개 dict → 914 MB

원인:
  1) numeric_count() 가 컬럼마다 전체 데이터 행을 다시 스캔 → to_float 2배 호출
  2) 컬럼 상수인 test_number/unit/limits 를 셀마다 재계산
  3) record-dict 구조 자체 (가장 큼 — 1)2) 만 고치면 1.5배)

컬럼형으로 바꾸면 파싱 10초 → 0.65초, 메모리 914MB → 24MB 로 측정되었다.

## 참고 구현
fast_datalog.py 에 컬럼형 파서, 열 일괄 파싱 고속 경로, DEVICE_ID 조인,
디스크 캐시, 벡터화 분석, 지연 상세 생성이 구현되어 있다. 참고 구현으로 삼되,
**기존 코드와 동작이 다르면 기존 코드가 정답**이다. 반드시 기존 로직에 맞춰라.

## 할 일

1. cdf_compare_tool.py 에 컬럼형 데이터 구조를 추가한다. 항목당:
     values: np.ndarray(float64)    — 측정값
     rows:   np.ndarray(int32)|None — 결측이 있을 때만 원본 행 인덱스 (자리 보존용)
     test_number / unit / lower_limit / upper_limit
   행 순서 정보(samples, device_ids, bins, sites)는 항목 공통으로 1벌만 보관한다.

2. 파싱 경로를 다시 쓴다.
   - numeric_count() 사전 스캔 제거. 열을 파싱하면서 유효값 유무를 판단한다.
   - 열 전체를 한 번에 파싱:
         try:  vals = list(map(float, column)); idx = None      # 고속 경로
         except ValueError: ...개별 처리하며 idx 기록...
   - 컬럼 상수 메타는 컬럼당 1회만 계산
   - 행 상수(sample, device_id, bin, site)는 전체 행에 대해 1회만 리스트로
   - **항목 탐지 규칙은 기존과 완전히 동일하게 유지한다.**
     find_row_with_label / find_test_item_name_row("비어있지 않은 셀이 가장 많은 행")
     / find_metadata_row_before_data / 메타 컬럼 판정 / first_item_col 계산.
     여기가 바뀌면 항목 집합이 달라져 결과가 어긋난다.
   - B단계에서 만든 DEVICE_ID 처리(식별자 보관 + 항목에서 제외)를 유지한다.

3. 분석 경로를 컬럼형 위에서 벡터화한다.
   - item_analysis() 가 post_records 를 enumerate 하면서 post_values[index] 를
     참조하는 구조를 없앤다. rows 인덱스로 자리를 보존하면 오정렬이 원천 차단된다.
   - Pre↔Post 조인은 **DEVICE_ID 기준**을 유지한다 (B단계 결과).
   - 통계 정의는 그대로: 표준편차 ddof=0, 임계 FLAG_LIMIT=3, diff = post/pre - 1.
     **이 단계에서 통계를 개선하지 마라.**
     ddof 와 임계값을 함수 인자로 노출하되 기본값은 현재 동작과 동일하게.

4. record-dict 가 필요한 곳을 위한 어댑터:  materialize_records(dataset, items=[...])
   - fail 분석 경로(rows_from_records, merged_fail_rows)와 XLSX 익스포트가 쓴다.
     **필요한 항목만** 만들어 넘겨라.
   - 전 항목을 materialize 하는 호출이 남아 있으면 안 된다. grep 으로 확인해라.

5. 구버전 Tkinter GUI(CdfCompareApp)는 건드리지 마라. NOTES.md 에만 기록.

## 완료 기준
- regression_check PASS  ← 이 단계에서 가장 중요하다.
  하나라도 불일치하면 원인을 찾을 때까지 다음으로 넘어가지 마라.
- bench.py 에서 extract 구간이 10배 이상 단축
- 최대 메모리가 크게 감소
- XLSX 익스포트 결과가 이전과 동일 (파일을 열어 몇 개 셀 비교)
```

---

## §4. 요약 먼저 push (체감 속도)

```text
프로젝트 루트의 CLAUDE.md 를 읽고 시작해줘.

분석 결과를 단계적으로 내보내서 대기 체감을 줄여줘.

## 현재
분석이 100% 끝나야 화면에 아무것도 안 뜬다.
그런데 요약표(results)는 상세(details)보다 훨씬 먼저 준비된다.

## 할 일

1. 서버: 요약이 준비된 시점에 먼저 노출한다.
   - run_analyze_job / run_fail_analyze_job 에서 results 와 selected_summary 가
     완성되면 그 시점에 CURRENT_PAYLOADS[mode] 를 채우고
     set_current_analysis_status(mode, 'partial', ...) 로 표시
   - 상세/캐시 저장은 그 뒤에 계속 진행하고, 끝나면 'done' 으로
   - GET /latest-analysis 는 status 가 'partial' 이면 200 으로 요약 payload 를
     반환하되 payload['partial'] = True 를 넣는다

2. 프런트엔드 (HTML 문자열 — 이 단계에서만 수정 허용):
   - 폴링 중 partial payload 를 받으면 즉시 요약표를 렌더
   - 상단에 "상세 준비 중" 표시, done 이 오면 조용히 갱신
   - 항목 클릭 시 상세가 없으면 /item 을 호출 (이미 구현돼 있음)

3. 진행률을 실제 비중에 맞춘다.
   현재 25/38/70/76/84 하드코딩인데 실제로는 파싱이 70%를 차지한다.
   파싱 구간에서 파일 바이트 진행률 또는 처리한 컬럼 수 비율을 흘려보내라.

4. 조건 선택 시 프리페치 (효과 큼)
   - item / readout 까지 선택되면 대상 파일이 확정된다
   - 그 시점에 데몬 스레드로 파싱 캐시를 미리 채운다 (§2 의 디스크 캐시 사용)
   - 진행 중인 프리페치는 경로 기준으로 dedup

## 완료 기준
- regression_check PASS
- Analyze 클릭 후 요약표가 뜨는 시점이 전체 완료보다 확연히 빠르다
- 상세가 아직 없는 항목을 클릭해도 정상 동작한다
```

---

## §5. Total 분석 병렬화 수정

측정: 현재 ThreadPool 은 **0.89배(역효과)**. ProcessPool 은 코어 수만큼 스케일.

```text
프로젝트 루트의 CLAUDE.md 를 읽고 시작해줘.

Total 분석의 병렬 처리가 실제로는 효과가 없는 문제를 고쳐줘.

## 측정 결과 (2코어, 동일 작업 4회)
  순차           2.41s   (1.00배)
  ThreadPool(4)  2.72s   (0.89배)  ← 현재 구현. 순차보다 느리다
  ProcessPool(4) 1.38s   (1.75배)

원인: run_total_analyze_job() (6713줄) 이 ThreadPoolExecutor(6726줄) 를 쓰는데
파싱이 순수 파이썬이라 GIL 을 놓지 않는다. 스레드 전환과 GIL 경합 오버헤드만 붙는다.

## 할 일

1. ThreadPoolExecutor → ProcessPoolExecutor
   - mp_context 는 'spawn' 명시 (Windows 기본이자 PyInstaller 안전)
   - max_workers = max(1, min(os.cpu_count() or 1, len(combos)))
     현재 상한 4 는 제거하되 환경변수로 조정 가능하게

2. PyInstaller 대응
   - main() 진입부(또는 if __name__ == '__main__' 최상단)에
     multiprocessing.freeze_support() 호출
   - 이 exe 에는 pyi_rth_multiprocessing 훅이 이미 번들되어 있어
     추가 hidden-import 없이 동작할 것이다. **빌드 후 exe 로 반드시 실측 확인.**

3. 워커 함수를 picklable 하게
   - 워커에는 원시 인자(파일 경로 문자열, 조합 dict, 모드 문자열)만 넘긴다
   - 워커는 **요약 rows 와 캐시 파일 경로만** 반환한다.
     payload 전체를 pickle 로 되돌리면 직렬화 비용 때문에 병렬 이득이 사라진다
   - 상세는 워커가 캐시 파일로 쓰고, 부모는 경로만 받아 manifest 를 만든다
   - 클로저/람다/전역 상태에 의존하지 않는 모듈 최상위 함수로 정의

4. 실패한 조합을 조용히 버리지 마라 (6744줄의 `except Exception: continue`)
   - 실패 조합 목록을 payload['skipped_conditions'] 로 반환
   - 완료 메시지에 "분석 실패 조건 N건" 포함
   - 예외 트레이스백은 로그 파일에

5. 폴백
   - ProcessPool 생성이 실패하면 순차 실행으로 자동 폴백하고 경고를 남긴다.
     절대 크래시로 끝나면 안 된다.

## 완료 기준
- regression_check PASS
- Total 분석 시간이 코어 수에 비례해 감소
- **PyInstaller 로 빌드한 exe 에서 정상 동작** ← 반드시 확인
- 일부러 잘못된 파일을 섞었을 때 "분석 실패 조건 1건" 이 표시된다
```

---

## §6. 프론트엔드 렌더 최적화

```text
프로젝트 루트의 CLAUDE.md 를 읽고 시작해줘.

결과가 뜬 뒤의 화면 조작(정렬/필터/항목 클릭/차트)이 느린 문제를 고쳐줘.
대상은 cdf_compare_web.py 55~4664줄 HTML 문자열 안의 JS 다.
이 단계에서는 HTML 문자열 수정을 허용한다.

## 고칠 것 (측정된 병목 순)

1. 요약표 통계 재계산 제거
   summaryCellValue() 가 avg/stdev/min/max 셀마다 valuesForSummaryRow() +
   statsForValues() 를 호출해 전체 샘플을 다시 훑는다. 정렬은 비교 함수마다
   2번씩 호출하므로 n log n 배로 악화된다.
   → applyAnalysisPayload() 시점에 항목별 통계를 1회 계산해 캐시.
     정렬은 decorate-sort-undecorate 로.

2. Math.min(...arr) / Math.max(...arr) 스프레드 제거
   statsForValues, drawChart, drawDiffCdfChart, renderOverTable,
   drawScatterPlot, drawPpfChart 에 있다.
   인자 수가 엔진 한계를 넘으면 RangeError 로 차트가 통째로 죽는다.
   → 단순 for 루프 헬퍼로 교체

3. CDF 분위수 O(n²) 제거
   cdfFraction() 이 점마다 전체 배열을 선형 스캔. n=3,000이면 900만 비교/프레임.
   → 값 배열을 한 번 정렬해 캐시하고 이진 탐색(lowerBound)

4. 표 렌더링
   renderSummaryListTable / renderDetailTable / renderOverTable 이 매번
   innerHTML='' 후 전 행 재생성 + 행마다 onclick 클로저와 체크박스 리스너 생성.
   → tbody 에 리스너 1개 + data-* 속성으로 이벤트 위임
   → 행이 300개를 넘으면 가상 스크롤 또는 페이지네이션

5. 차트 렌더
   - drawCharts() 가 활성 탭과 무관하게 캔버스 5개를 매번 전부 그린다
     → 활성 탭만, 탭 전환 시 lazy 렌더
   - window resize 핸들러에 150ms 디바운스 (현재 없음)
   - chartPixelRatio() 가 min(4, DPR*2) 라 캔버스 5개 합계가 200MB 초과
     → min(DPR, 2). 클립보드 복사 시에만 고배율 오프스크린

6. getCss() 호출 위치
   drawScatterPlot 의 rows.forEach 안에서 포인트마다 getComputedStyle 을 부른다
   → 모듈 초기화 시 팔레트를 1회 읽어 상수로 캐시

## 하지 말 것
- 판정 로직, 표시 값, 컬럼 구성, 색상 규칙을 바꾸지 마라. 순수 성능 작업이다.
- 프레임워크나 차트 라이브러리를 도입하지 마라. 외부 의존성 0 유지.

## 완료 기준
- 1,000행 요약표에서 헤더 클릭 정렬이 체감상 즉시 반응
- 유닛 3,000개 항목을 선택해도 차트가 RangeError 없이 그려진다
- 창 크기 조절 중 프리즈가 없다
- 표시되는 숫자와 차트 모양이 이전과 동일 (스크린샷 비교)
```

---

## §7. 이번 작업에서 손대지 말 것

에이전트가 "겸사겸사" 고치려 들 수 있는 것들입니다. 성능 작업과 섞으면 검증이
불가능해집니다. `NOTES.md` 에 정리해 뒀으니 나중에 별도 브랜치로 진행하세요.

| 항목 | 왜 나중에 |
|---|---|
| **재시험 이력 보존 → Intermittent 판정** | 판정 결과 변경. **정확성 최우선 과제** |
| `sample_std` 를 ddof=1 로 | 모든 숫자가 바뀜 |
| `n < 11` 에서 `INSUFFICIENT N` | 판정 결과 변경 |
| `diff_ratio` 부호/폭발 수정 | 판정 결과 변경 |
| `safe_ratio`/`sigma`/`mean` 의 `0.0` → `None` | 판정 결과 변경 |
| `unit_is_allowed` 화이트리스트 확대 | 항목 집합 변경 |
| CSRF/Host 검증 | 별도 보안 작업 |
| `CdfCompareApp` 670줄 정리 | 성능과 무관 |
| Wafer Map / PPF 차트 / Fail 오버레이 | 기능 판단 필요 |

---

## §8. 커밋 메시지 템플릿

```text
perf(payload): SELECT 항목만 details 생성, /progress 슬림화

- payload_with_items 가 selected_summary 항목만 순회하도록 변경
- /progress 는 상태만 반환, JOBS 에 payload 미저장
- 측정: json.dumps 12.5s → 0.7s, payload 839MB → 25MB
- regression_check PASS
```
