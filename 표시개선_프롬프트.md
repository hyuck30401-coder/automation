# CDF Compare Tool — 표시 개선 프롬프트 (Summary 3열 제거 + Fail 모드 Pre 조인 복구)

두 가지 작업입니다. **§U1 은 표시만 바꾸고, §U2 는 지금 비어 있는 값을 채웁니다.**

```powershell
git checkout perf/columnar          # 또는 이 작업의 기준 브랜치
git checkout -b ui/summary-and-pre-join
```

> 통계 작업(`stats/threshold`)과는 **별개 브랜치**로 가세요. §U2 가 `diff_s` 를 만들어내므로
> 두 작업을 섞으면 판정 변화의 원인이 통계 임계 때문인지 Pre 조인 때문인지 구분이 안 됩니다.

---

## §U2 를 왜 하는가 — Fail 모드 Pre 열이 **전부 비어 있습니다** (실측 확인)

실제 Test Data 를 직접 대조한 결과입니다.

```
PRE  n=3173   Serial # 예: 100041, 100049, 100057 ...   DEVICE_ID 예: 224838356 ...
POST n= 145   Serial # 예: 1, 2, 3, 4, 5 ...            DEVICE_ID 예: 214921241 ...

Serial #    겹침 :   0 / 79      ← 전혀 안 맞음
DEVICE_ID   겹침 :  78 / 78      ← 100% 일치
```

`8224314 feat(B)` 커밋에서 Pre↔Post 조인을 DEVICE_ID 로 바꿨지만, 그 수정은
`record_sample_id()` / `records_by_sample()` 를 타는 **Pass 경로만** 고쳤습니다.
Fail 경로는 `rows_from_records()` 를 타는데, 이 함수는 아직 **Serial # 로 행을 만듭니다.**

```python
# web.py:5338  rows_from_records() — 현재 코드
sample = str(record.get("sample", "")).strip()          # ← Serial #. DEVICE_ID 가 아님
row_map[sample] = {"source_sample": sample, ...}
```

```python
# web.py:5823  analyze_fail_to_json() — Pre 를 Serial # 로 찾는다
pre_by_sample = {
    str(row.get("source_sample", "")).strip() or str(index): row
    for index, row in enumerate(pre_rows, start=1)
}
...
pre_record = pre_by_sample.get(sample, {}).get("items", {}).get(item)    # 항상 None
```

Post 의 Serial # 는 `1,2,3...`, Pre 의 Serial # 는 `100041,100049...` 이므로
**`pre_by_sample.get(sample)` 은 100% 실패**합니다. 그 결과 Fail 모드에서:

| 영향 받는 것 | 현재 상태 |
|---|---|
| Detail 테이블 `Pre` 열 | **전부 빈칸** |
| Detail 테이블 `Delta` 열 (`post/pre − 1`) | `pre=None` → **전부 빈칸** |
| Detail 테이블 `σ (Delta)` 열 | **전부 빈칸** |
| `Spec.-Out Type` 의 `Measured + Delta` / `Delta` 분류 | **절대 안 나옴** (Measured 만 나옴) |
| `fail_type` 의 `Slight` (`\|mea_s\|>3 and \|diff_s\|>3`) | **절대 안 나옴** → 전부 `Excessive`/`Tail` 로 몰림 |
| Scattered Plot | `diff_s` 가 없어 **점이 하나도 안 찍힘** |
| Delta CDF | **빈 그래프** |
| Excel Raw 시트 `Pre`/`Delta`/`Diff_S` 열 | **전부 빈칸** |

즉 Fail 분석의 "열화량" 축이 통째로 죽어 있습니다.

---

## §U1. Summary 테이블에서 Total 전용 3열 제거

```text
프로젝트 루트의 CLAUDE.md 를 읽고 시작해줘.

Summary 테이블(resultTable, "Fail & Abnormal Data Lists")에서 Total Analysis 일 때만
앞에 붙는 3열(Reliability / FT Temp. / Read-out)을 제거해줘. 항상 7열 고정으로 만든다.

## 고칠 곳 (실제 위치는 grep 으로 다시 확인할 것)
1. HTML 문자열 안의 JS  resultColumnsForPayload()  — payload.total_analysis 일 때
   [["reliability_item","Reliability"],["ft_temp","FT Temp."],["readout","Read-out"], ...columns]
   를 prepend 하는 분기를 없애고 항상 columns 를 그대로 반환한다.
2. 같은 파일의  resultColumns()  — 동일한 prepend 분기가 하나 더 있다. 같이 없앤다.
3. Python  export_summary_rows()  — keys 리스트 앞의
   "reliability_item", "ft_temp", "readout" 3개를 뺀다.
4. Python  build_raw_export_workbook()  의 summary_rows 헤더 —
   "Reliability", "FT Temp.", "Read-out" 3개를 뺀다.
   → 3번과 4번은 열 개수가 반드시 같아야 한다. 헤더와 데이터가 어긋나면 안 된다.

## 건드리지 말 것
- Raw 시트(EXPORT_RAW_COLUMNS, export_detail_rows)는 그대로 둔다.
  Raw 는 조건별 재분석용 원본이라 Reliability/FT Temp./Read-out 이 없으면
  Total 분석 결과에서 행을 조건별로 되돌릴 수가 없다.
- rowMatchesAnalysisFilters / rowMatchesPayloadFilters 의 필터 동작은 그대로 둔다.
  열만 없애는 것이지, 필터링 자체를 없애는 게 아니다.
- selected_summary 페이로드에서 reliability_item / ft_temp / readout 필드를 지우지 마라.
  필터와 그래프(graphSourcePayloads, graphTempMatches)가 이 값을 쓴다. 표시만 뺀다.

## 확인
- CSS 의 body.results-window #resultTable th:nth-child(1)~(7) 폭 지정이 이미 7열
  기준이다. 3열이 빠지면 오히려 폭이 맞게 된다. CSS 는 수정 불필요 — 확인만 해라.

## 완료 기준
- Total Analysis 를 돌려도 Summary 테이블 헤더가
  Test No. / Item / Unit / LL / UL / Avg. / Stdev. / Min. / Max. / Q'ty / Sample No.
  11열로만 나온다  (※ 실제 passColumns 는 11개다. "7열"은 CSS nth-child 지정 개수다)
- Reliability 필터 드롭다운을 바꾸면 여전히 행이 필터링된다
- Excel Summary 시트 헤더와 데이터 열 개수가 일치한다
- 그래프의 온도별 시리즈 표시가 그대로 동작한다
```

---

## §U2. Fail 모드 Pre 를 DEVICE_ID 로 조인 (핵심)

```text
프로젝트 루트의 CLAUDE.md 를 읽고 시작해줘. §3-3 (DEVICE_ID 조인) 을 특히 잘 읽어라.

Fail 모드 Detail 테이블의 Pre / Delta / σ(Delta) 가 전부 비어 있다.
원인은 Fail 경로가 아직 Serial # 로 Pre 를 찾기 때문이다. DEVICE_ID 로 바꿔라.

## 실측 근거 (내가 직접 확인한 값이다. 재현해서 먼저 확인해라)
Test Data 의 Pre/Post 파일을 열어 47행에서 DEVICE_ID 컬럼을 찾고 52행 이후를 비교하면
  Serial #  겹침 :  0 / 79
  DEVICE_ID 겹침 : 78 / 78
이다. 이 숫자를 직접 재현해서 로그로 보여준 다음 작업을 시작해라.

## 설계 — 표시용 키와 조인용 키를 분리한다
Detail 테이블의 "Sample No." 는 지금처럼 Serial # 를 그대로 보여줘야 한다.
따라서 sample_states 의 키(=source_sample)는 바꾸지 말고, 조인 전용 필드를 하나 더 만든다.

1. rows_from_records() — 행을 만들 때 join_key 를 함께 넣는다
     row_map[sample] = {
         "source_sample": sample,                    # 표시용 Serial # (변경 없음)
         "join_key": record_sample_id(record),       # 조인용 DEVICE_ID (신규, 없으면 Serial #)
         "bin": ...,
         "items": {},
     }
   ※ record_sample_id() 는 이미 DEVICE_ID 우선 + Serial # 폴백으로 되어 있다. 그대로 쓴다.
   ※ 나중에 row_map[sample] 에 다른 item 레코드를 채울 때, join_key 가 비어 있으면
     그때 채워준다 (bin 을 채우는 기존 로직과 같은 방식).

2. merged_fail_rows() — sample_states 에 join_key 를 실어 나른다
     sample_states[sample] = {
         "sample": sample,
         "serial": sample,
         "join_key": row.get("join_key", ""),        # 신규
         ...
     }
   재시험 stage 병합(zip(previous_retest_samples, rows))은 그대로 둔다.

3. analyze_fail_to_json() — pre_by_sample 을 join_key 로 만든다
     pre_by_sample = {
         (row.get("join_key") or str(row.get("source_sample", "")).strip() or str(index)): row
         for index, row in enumerate(pre_rows, start=1)
     }

4. 같은 함수 안의 Pre 조회 2곳을 join_key 로 바꾼다
   (a) fail_samples 루프    — pre_values_by_sample[sample] 채우는 곳
   (b) pass_samples 루프    — pass_pairs / normal_details 만드는 곳
     join_key = sample_states[sample].get("join_key") or sample
     pre_record = pre_by_sample.get(join_key, {}).get("items", {}).get(item)
   ※ 두 곳 다 고쳐야 한다. (b) 를 빠뜨리면 Pass 기준선(pass_diff_values)이 계속 비어서
     Delta CDF 에 "Pass Diff data unavailable" 경고가 그대로 남는다.

5. 매칭 결과를 payload["match_summary"] 로 반환한다
     {"key": "device_id", "post_units": N, "matched": M, "unmatched": N-M}
   Pass 모드의 match_summary_for_files() 와 같은 형식으로 맞춰라.
   그리고 payload["message"] 뒤에 "Pre 매칭 M/N" 을 붙여 화면에 보이게 한다.

## 주의
- Serial # 폴백을 없애지 마라. DEVICE_ID 가 없는 사업장 데이터가 들어올 수 있다.
  join_key 가 비면 Serial # 로 떨어지는 동작이 유지돼야 한다.
- Detail 테이블 "Sample No." 열에 DEVICE_ID 가 뜨면 잘못 고친 것이다. Serial # 가 떠야 한다.
- items_over_sigma_by_sample / over_sigma 의 sample 값도 표시용이므로 바뀌면 안 된다.
- CLAUDE.md §5-3, §5-4 를 지켜라. 실제 Test Data 로만 검증하고,
  폴더를 복사하거나 이름을 바꾸는 식의 우회는 금지다.

## 완료 기준 — 숫자로 보고할 것
1. Fail 분석 후 match_summary 가 matched 78 / post_units 78 (또는 79) 로 나온다
2. Detail 테이블 Pre 열이 채워진 행 수 / 전체 행 수 를 세어 보고한다. 0 이면 실패다.
3. Delta 열과 σ(Delta) 열도 같이 채워진다
4. Spec.-Out Type 에 "Delta" 또는 "Measured + Delta" 가 최소 1건 이상 나타난다
   (수정 전에는 0건이었다. 0건 그대로면 조인이 안 된 것이다)
5. Scattered Plot 에 점이 찍힌다 (수정 전 0개)
6. Delta CDF 의 "Pass Diff data unavailable" 경고가 사라진다
7. Excel Raw 시트의 Pre / Delta / Diff_S 열이 채워진다
8. Pass 모드 결과는 **하나도 바뀌면 안 된다** — 기존 회귀 스냅샷으로 확인해라
```

---

## 검증 순서

```powershell
# 1. §U2 먼저 (값이 채워지는 작업). Pass 모드가 안 바뀌는지 회귀로 확인
python -m pytest tests -q

# 2. 실데이터로 Fail 분석 → 위 완료 기준 1~7 확인
#    ※ 개발 서버 띄우기 전에 exe 가 8765 를 잡고 있지 않은지 반드시 확인 (CLAUDE.md §5-3-1)
python cdf_compare_web.py

# 3. §U1 (표시만 바꾸는 작업) 은 그 다음에
```

---

## 커밋

```powershell
git add -A
git commit -m "fix(fail): Pre 조인을 DEVICE_ID 기준으로 복구 — Pre/Delta/Diff_S 값 살림"
git commit -m "feat(ui): Summary 테이블에서 Total 전용 3열(Reliability/FT Temp./Read-out) 제거"
```

---

## 참고 — 이 작업 후 통계 브랜치에 미치는 영향

§U2 로 `diff_s` 가 처음으로 값을 갖게 되므로, **Fail 모드의 SELECT/`fail_type` 분포가 바뀝니다.**
`stats/threshold` 브랜치의 baseline(`tests/verdict_base/`)을 이미 저장해 뒀다면
**이 작업을 머지한 뒤 baseline 을 다시 만드세요.** 안 그러면 Grubbs 임계 도입의 효과와
Pre 조인 복구의 효과가 한 리포트에 섞여 나옵니다.
