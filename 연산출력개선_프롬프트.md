# CDF Compare Tool — 연산·출력 개선 프롬프트 (V1~V6)

`표시개선_프롬프트.md`(§U1/§U2)를 **머지한 뒤에** 시작하세요.
두 작업이 `passColumns`, `export_summary_rows()`, `selected_summary_rows()` 를 같이 건드려서
동시에 진행하면 충돌합니다.

```powershell
git checkout perf/columnar
git merge ui/summary-and-pre-join          # §U1/§U2 머지 확인
git checkout -b ui/analysis-output
```

---

## 왜 하는가 — 한 줄 요약

| | 문제 | 결과 |
|---|---|---|
| V1 | 유닛 수 `N` 이 화면에 없다 (`Q'ty` 는 SELECT 개수) | `Q'ty 5` 가 145개 중 5개인지 3,000개 중 5개인지 알 수 없다 |
| V2 | Pre→Post **집단 전체 이동**을 보는 칸이 없다 | 전 유닛이 균일하게 열화하면 `Mea_S`·`Diff_S` 가 둘 다 0 → **OK 판정** |
| V3 | `Avg./Stdev./Min./Max.` 를 프론트가 재계산한다 | 항목을 클릭해야 채워지고, Excel 에는 **항상 빈칸** |
| V4 | SELECT 사유가 항목 목록에 없다 | 300개 항목을 하나씩 눌러봐야 triage 가 된다 |
| V5 | `mea_s`/`diff_s` 를 백엔드·프론트가 **각자 계산**한다 | Detail 테이블 숫자와 Scatter 점 위치가 다르다 |
| V6 | CDF 가 1,200점 넘으면 균등 솎아낸다 | 이상치를 찾는 툴인데 **이상치 점이 사라진다** |

**V2 를 조금 더 설명합니다.** `Mea_S`, `Diff_S` 는 둘 다 자기 집단 안에서 표준화합니다.

```
Mea_S  = (x    − Post 평균)  / Post σ
Diff_S = (diff − diff 평균) / diff σ        ← diff 를 0 과 비교하는 게 아니라 남의 diff 와 비교
```

145개가 전부 −5% 씩 밀리면 `diff_mean = −0.05`, 각 유닛의 `diff − diff_mean = 0` → **`Diff_S` 전부 0**.
Post 분포가 통째로 이동해도 내부 상대 위치는 그대로라 `Mea_S` 도 변화 없음. 판정은 `OK` 입니다.

그런데 그 정보는 **이미 계산돼 있습니다.** `item_analysis()` 의 `result_row` 에
`pre_mean`, `pre_sigma`, `post_mean`, `post_sigma`, `diff_mean`, `diff_sigma`, `n_pre`, `n_post`
가 전부 들어 있고 payload 로 브라우저까지 갑니다. **`passColumns` 에 없어서 안 그려질 뿐입니다.**

→ V1·V2·V3 는 **새로 계산할 게 없습니다.** 있는 값을 summary row 에 얹고 열을 추가하는 작업입니다.

---

## 진행 순서

| 단계 | 작업 | 판정 변경 | 난이도 |
|---|---|---|---|
| **V1+V2+V3** | summary row 필드 확장 + 열 추가 (한 묶음) | 없음 | 낮음 |
| **V4** | Reason 열 + 심각도 기본 정렬 | 없음 | 낮음 |
| **V5** | σ 계산 백엔드 단일화 | 없음 (표시만 일치) | **높음** |
| **V6** | CDF 이상치 강제 표시 | 없음 | 낮음 |

**V1~V3 을 한 커밋으로 묶으세요.** 셋 다 `selected_summary_rows()` 와 `passColumns` 를 건드려서
따로 하면 같은 파일을 세 번 고치게 됩니다.

---

## §V1+V2+V3. Summary 테이블 — 있는 값을 화면에 꺼내기

```text
프로젝트 루트의 CLAUDE.md 를 읽고 시작해줘.
§5-1(판정 숫자를 바꾸지 말 것)은 이 작업에 그대로 적용된다.
표시할 값을 늘리는 작업이지, 판정 로직은 한 줄도 건드리지 않는다.

## 배경
item_analysis() 의 result_row 에 n_pre, n_post, pre_mean, pre_sigma, post_mean,
post_sigma, diff_mean, diff_sigma 가 이미 있다. payload 로 브라우저까지 가는데
passColumns 에 없어서 화면에 안 나온다. 이걸 꺼내는 작업이다.

## V1 — 유닛 수 N
1. selected_summary_rows() 가 만드는 dict 에 아래를 추가한다.
     "n": row.get("n_post")          # 전체 Post 유닛 수
     "n_pre": row.get("n_pre")
   ※ row 는 app.results 의 원소이므로 result_row 필드를 그대로 쓸 수 있다.
2. Q'ty 옆에 비율을 계산해 넣는다.
     "qty_ratio": len(samples) / n_post   (n_post 가 0 이면 None)
3. passColumns 에 열을 추가한다.
     ["n", "N"]          → Test No. / Item 다음, Unit 앞
     ["qty_ratio", "%"]  → Q'ty 바로 뒤
   qty_ratio 는 백분율로 표시한다 (0.034 → "3.4%"). fmt() 를 고치지 말고
   summaryCellValue() 안에서 key === "qty_ratio" 일 때만 포맷해라.

## V2 — 집단 전체 이동 (Shift)
1. selected_summary_rows() 에 추가한다.
     "shift"       = post_mean - pre_mean                       (pre 없으면 None)
     "shift_sigma" = (post_mean - pre_mean) / pre_sigma         (pre_sigma 가 0/None 이면 None)
     "diff_mean"   = row.get("diff_mean")                       (평균 열화율)
   ※ 0.0 을 반환하지 마라. 계산 불가면 None 이다 (NOTES.md P0 통계 정의 참조).
2. passColumns 에 추가한다.
     ["shift", "Shift"]            → Stdev. 뒤
     ["shift_sigma", "Shift/σ"]    → Shift 뒤
     ["diff_mean", "Δ Mean"]       → Shift/σ 뒤
   Δ Mean 은 백분율 표시 (-0.05 → "-5.00%").
3. Fail 모드(analyze_fail_to_json 의 summary_rows)에도 같은 필드를 넣어라.
   Fail 모드는 pass 집단 기준으로 post_mean/post_sigma/diff_mean 을 이미 계산하고 있으니
   그 값을 쓰면 된다. Pre 통계가 없으면 None 으로 둔다.

## V3 — Avg./Stdev./Min./Max. 를 백엔드로 이관
현재 프론트 summaryCellValue() 가 이 4개를 가로채서 statsForValues() 로 재계산한다.
그래서 (a) 항목을 클릭해 /item 을 받기 전에는 빈칸이고 (b) Excel Summary 시트는 항상 빈칸이다.

1. selected_summary_rows() 에서 백엔드가 계산해 넣는다.
     "avg"    = post_mean
     "stdev"  = post_sigma
     "min"    = min(post_values)   (비어 있으면 None)
     "max"    = max(post_values)
   ※ 지금 프론트는 리드아웃이 있으면 T1/T2/T3 값을 flatMap 으로 합쳐서 평균을 낸다.
     168hr/500hr/1000hr 분포가 다른 게 정상인데 섞으면 의미가 없다.
     백엔드는 **기준 리드아웃(post_values) 하나만** 쓴다. 섞지 마라.
2. summaryCellValue() 에서 ["avg","stdev","min","max"] 를 가로채는 분기를 삭제한다.
   row[key] 를 그대로 반환하게 한다.
3. valuesForSummaryRow() / statsForValues() 가 다른 곳에서 안 쓰이면 삭제한다.
   쓰이면 남겨두되 summary 경로에서는 호출하지 않는다.

## 내보내기
- export_summary_rows() 의 keys 에 n, avg, stdev, min, max, shift, shift_sigma,
  diff_mean, qty_ratio 를 화면과 같은 순서로 반영한다.
- build_raw_export_workbook() 의 summary_rows 헤더도 같은 개수·같은 순서로 맞춘다.
  헤더 개수와 데이터 개수가 어긋나면 안 된다.
- Raw 시트(EXPORT_RAW_COLUMNS, export_detail_rows)는 건드리지 마라.

## CSS
- 메인 창은 .table-wrap { overflow:auto } + table { width:max-content } 라 열이 늘어도 괜찮다.
- 별도 결과창(body.results-window #resultTable th:nth-child(1)~(7))만 고정 폭이 박혀 있다.
  늘어난 열 개수에 맞춰 다시 잡거나, 그 nth-child 블록을 지우고 가로 스크롤에 맡겨라.

## 완료 기준 — 숫자로 보고할 것
1. Test Data 로 Pass 분석 → Summary 테이블에서 N 열이 145 로 나온다
2. 항목을 클릭하기 **전에** Avg./Stdev./Min./Max. 가 이미 채워져 있다 (지금은 빈칸)
3. Shift / Shift/σ / Δ Mean 이 채워진 항목 수 / 전체 항목 수 를 보고한다
4. Excel Summary 시트에 Avg./Stdev./Shift 값이 실제로 들어간다 (지금은 빈칸)
5. **판정 결과(SELECT 항목 수)가 수정 전과 동일하다.** pytest 회귀 통과.
   달라지면 판정 로직을 건드린 것이니 되돌려라.
```

---

## §V4. SELECT 사유 표시 + 심각도 기본 정렬

```text
프로젝트 루트의 CLAUDE.md 를 읽고 시작해줘. 판정 로직은 건드리지 않는다.

## 문제
어떤 항목이 왜 SELECT 됐는지(측정값이 튄 건지, 변화량이 튄 건지)가 Detail 테이블에만 있다.
Summary 목록에는 없어서, 항목이 300개면 하나씩 눌러봐야 한다.
게다가 기본 정렬이 Test No. 라 심각한 항목을 찾으려면 매번 헤더를 클릭해야 한다.

## 할 일
1. selected_summary_rows() 에 추가한다 (result_row 에 이미 있는 값이다).
     "max_mea_s"  = row.get("mea_s")      # 항목 내 최대 |Mea_S|
     "max_diff_s" = row.get("diff_s")     # 항목 내 최대 |Diff_S|
     "severity"   = max(|max_mea_s|, |max_diff_s|)   # 둘 다 None 이면 None
     "reason"     = 아래 규칙
        둘 다 임계 초과      → "Measured + Delta"
        Mea_S 만 초과        → "Measured"
        Diff_S 만 초과       → "Delta"
        그 외                → ""
   ※ 임계값은 지금 FLAG_LIMIT(=3)을 쓴다. 하드코딩하지 말고 FLAG_LIMIT 을 참조해라.
     나중에 stats/threshold 브랜치에서 Grubbs 로 바뀌면 여기가 자동으로 따라가야 한다.
   ※ 프론트의 detailSpecOutType() 이 같은 분류를 하드코딩 3 으로 하고 있다.
     같은 규칙이 두 군데 있으면 안 되니, detailSpecOutType() 도 백엔드가 내려주는
     임계값을 참조하도록 바꿔라 (payload 에 "flag_limit" 를 실어 보내면 된다).

2. passColumns 에 추가한다.
     ["reason", "Reason"]          → Item 뒤
     ["severity", "Max |σ|"]       → Q'ty 앞
   Max |Mea_S| / Max |Diff_S| 는 열이 너무 늘어나므로 Reason + severity 두 개로 대신한다.
   두 원본 값은 payload 에는 실어 두되 화면 열로는 만들지 마라 (나중에 필요하면 꺼내 쓴다).

3. 기본 정렬을 severity 내림차순으로 바꾼다.
     let sortState = { column: null, reverse: false };   // 1942행 부근
   을 severity 기준으로 초기화하고, 분석 결과를 새로 적용할 때
   리셋하는 두 곳(2738, 2752행 부근)도 같이 맞춘다.
   사용자가 헤더를 클릭해 다른 열로 정렬하는 동작은 그대로 유지한다.

## 완료 기준
- Summary 목록 첫 행이 |σ| 가 가장 큰 항목이다
- Reason 열에 Measured / Delta / Measured + Delta 가 실제로 분포한다
  (전부 같은 값이면 분류 로직이 잘못된 것이다)
- Detail 테이블의 Spec.-Out Type 과 Summary 의 Reason 이 같은 임계값을 쓴다.
  코드에서 하드코딩 3 을 grep 해서 남아 있는 곳이 없는지 보고해라.
- 판정 결과(SELECT 항목 수)는 수정 전과 동일하다
```

---

## §V5. σ 계산을 백엔드로 단일화 — 가장 큰 작업

```text
프로젝트 루트의 CLAUDE.md 를 읽고 시작해줘.

## 문제
백엔드가 details[].mea_s / diff_s 를 계산해서 보내는데,
프론트 readoutDetailSeries() 가 같은 값을 다시 계산한다.

  point.mea_s  = sigmaValue(point.post_value, postMean, postSigma);
  point.diff_s = sigmaValue(point.diff, diffMean, diffSigma);

모수가 다르다. 백엔드는 post_values 전체로, 프론트는 "선택된 리드아웃 시리즈 값들"로만
평균·σ를 낸다. 그래서 Detail 테이블에 mea_s = 3.4 로 찍힌 샘플이
Scattered Plot 에서는 ±3 박스 안에 들어가 있을 수 있다.
판정은 백엔드 값으로 하는데 사용자가 눈으로 검증하는 그림은 프론트 값이다.

## 할 일 — 계산은 백엔드에서만 한다
1. apply_post_readout_history_to_payload() 에서 post_readout_values 를 만들 때,
   시리즈마다 통계와 샘플별 σ 를 함께 계산해서 실어 보낸다.

     {
       "key": "post_t1",
       "label": "...",
       "values": [...],
       "stats": {"mean": ..., "sigma": ..., "diff_mean": ..., "diff_sigma": ..., "n": ...},
       "points": [
         {"sample": ..., "pre_value": ..., "post_value": ..., "diff": ...,
          "mea_s": ..., "diff_s": ...},
         ...
       ]
     }

   - diff 는 details 행의 pre_value 를 sample 기준으로 조인해서 계산한다
     (post/pre − 1). pre 가 없거나 0 이면 diff 는 None 이다.
   - σ 계산은 cdf_compare_tool.py 의 기존 함수(vector_stats/vector_sigmas)를 재사용해라.
     새 구현을 또 만들지 마라. 그게 지금 문제의 원인이다.
   - 계산 불가는 0.0 이 아니라 None 으로 둔다.

2. 프론트 readoutDetailSeries() 를 **읽기 전용**으로 바꾼다.
   - series.points 를 백엔드가 준 그대로 쓴다
   - sigmaValue / meanValue / populationStd 호출을 이 함수에서 전부 제거한다
   - 백엔드가 points 를 안 준 경우(구버전 캐시 등)에만 기존 방식으로 폴백하되,
     콘솔에 경고를 남긴다

3. 같은 재계산이 다른 곳에도 있는지 확인한다.
   diffFromPre / sigmaValue / populationStd / meanValue 호출부를 전부 grep 해서,
   "판정에 쓰이는 값"을 만드는 곳이 남아 있으면 같이 정리한다.
   ※ 그래프의 축 범위 계산(min/max, avg±6σ)처럼 순수 표시용 계산은 남겨도 된다.
     판정 숫자(mea_s / diff_s)를 만드는 곳만 없애는 것이다.

## 주의
- 판정 결과는 바뀌면 안 된다. 백엔드 값이 이미 정답이고, 프론트를 거기에 맞추는 작업이다.
- payload 크기가 커진다. points 를 SELECT 항목에만 넣어라
  (perf 작업에서 details 를 SELECT 항목에만 만든 것과 같은 원칙, 커밋 ef96d8d 참조).
- 성능 회귀를 확인해라. Perf Data 로 분석 시간을 재고 수정 전후를 비교해서 보고한다.

## 완료 기준 — 숫자로 보고할 것
1. 프론트에서 sigmaValue/populationStd/meanValue 로 mea_s·diff_s 를 만드는 코드가 0줄
   (grep 결과를 보여줘라)
2. Detail 테이블의 σ(Measured) 값과 Scattered Plot 의 Y 좌표가 일치한다
   — 임의의 샘플 3개를 골라 두 값을 나란히 출력해서 확인
3. 판정 결과(SELECT 항목 수, 항목별 result)가 수정 전과 완전히 동일하다. pytest 통과.
4. Perf Data 분석 시간이 수정 전 대비 +10% 이내
```

---

## §V6. CDF 그래프가 이상치를 지우지 않게

```text
프로젝트 루트의 CLAUDE.md 를 읽고 시작해줘. 그래프 표시만 바꾼다.

## 문제
drawCdf() 가 점이 1,200개를 넘으면 균등하게 솎아낸다.

  const pointStep = Math.max(1, Math.ceil(sorted.length / 1200));
  if (index % pointStep !== 0 && index !== sorted.length - 1) return;

3,000 유닛이면 pointStep = 3 → 3개 중 1개만 그린다. 정렬 배열에서 균등 샘플링이라
꼬리의 극단값이 인덱스 운에 따라 그냥 사라진다. 마지막 1개만 보장된다.
이상치를 찾는 툴에서 이상치가 안 그려지면 곤란하다.

## 할 일
drawCdf() 의 솎아내기 규칙을 바꾼다. 성능(1,200점 상한)은 유지하되 꼬리는 전부 그린다.

  - 정렬 배열의 **양쪽 끝 각각 min(50, n) 개는 무조건 그린다**
  - 가운데 구간만 pointStep 으로 솎는다
  - pointStep 은 "가운데 구간 길이 / (1200 - 양끝 개수)" 로 다시 계산한다

양끝 고정 방식을 쓰는 이유: 임계값(3σ / Grubbs)에 의존하지 않아서
나중에 stats 브랜치에서 임계가 바뀌어도 이 코드를 다시 안 고쳐도 된다.

## 완료 기준
- Perf Data(3,000 유닛)로 CDF 를 그렸을 때 CDF 0 근처와 1 근처의 점이 촘촘하다
- 그린 점 개수를 콘솔에 찍어서 1,200~1,300 사이인지 확인
- 그리기 시간이 수정 전과 체감 차이가 없다
```

---

## 커밋

```powershell
git commit -m "feat(summary): N/Shift/Avg 등 이미 계산된 통계를 Summary 테이블에 노출"
git commit -m "feat(summary): SELECT 사유(Reason) 열 추가 및 심각도 기본 정렬"
git commit -m "refactor(sigma): mea_s/diff_s 계산을 백엔드로 단일화 - 표와 그래프 값 일치"
git commit -m "fix(chart): CDF 점 솎아내기가 꼬리 이상치를 지우지 않도록 수정"
```

---

## stats/threshold 브랜치와의 관계

**이 작업을 먼저 하고 통계 작업으로 넘어가는 게 좋습니다.** 이유:

- **V1(N 열)은 §S3 의 선행 조건입니다.** Grubbs 임계는 n 의 함수라(n=145 → 3.506,
  n=3,000 → 4.299) n 이 화면에 없으면 사용자가 "왜 3.7 인데 OK 지?" 를 이해할 수 없습니다.
- **V4 가 하드코딩된 `3` 을 `FLAG_LIMIT` 참조로 정리합니다.** 지금 프론트에는 3 이
  최소 7군데 흩어져 있습니다(detailSpecOutType, sigma-fail 셀 강조, sigmaOver3,
  Scatter ±3 박스, Delta CDF ±3σ 점선, drawShift 표시 조건, fail_type 의 Slight).
  V4 에서 이걸 한 곳으로 모아두면 §S3 은 그 한 곳만 바꾸면 됩니다.
- **V5 가 프론트 재계산을 없앱니다.** 이걸 안 하면 §S3 에서 백엔드 임계를 Grubbs 로
  바꿔도 Scatter 는 계속 프론트가 만든 옛날 σ 로 그립니다.

이 작업을 머지한 **뒤에** `stats/threshold` 브랜치를 만들고 §S0 baseline 을 저장하세요.
