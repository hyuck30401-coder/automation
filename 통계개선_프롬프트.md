# CDF Compare Tool — 통계 정확성 개선 프롬프트 (별도 브랜치)

성능 작업(`perf/columnar`)과 **완전히 분리된 작업**입니다. 여기서는 **판정 숫자가 바뀝니다.**

```powershell
git checkout main            # 또는 성능 브랜치를 병합한 기준 브랜치
git checkout -b stats/threshold
```

---

## 왜 하는가 — 지금 판정은 양쪽 끝에서 모두 고장나 있습니다

`Mea_S` 는 자기 표본으로 표준화한 z-score인데, 임계가 **3으로 고정**되어 있습니다.
표본 크기를 전혀 반영하지 않아서 이런 일이 생깁니다.

| 유닛 수 n | 표본 내 최대 \|z\| (ddof=0) | 고정 3 판정 | 순수 노이즈 위양성률 (실측) |
|---:|---:|---|---:|
| 5 | 2.00 | **원리적으로 불가능** | 0.0% |
| 10 | 3.00 | **원리적으로 불가능** | 0.0% |
| 30 | 5.29 | 가능 | 5.0% |
| 145 | 11.96 | 가능 | **33.2%** |
| 1,000 | 31.59 | 가능 | **95.0%** |
| 3,000 | 54.75 | 가능 | **99.8%** |

- **소로트(5~10 유닛)** — 아무리 심한 이상치가 있어도 절대 flag 되지 않습니다. 화면엔 전부 "OK".
- **대로트(1,000+ 유닛)** — 정상 데이터인데도 거의 모든 항목이 SELECT로 뜹니다.

실제로 성능 벤치마크에서 **순수 정규분포 난수 1,000항목 중 999개가 SELECT**로 나왔습니다.
이건 툴이 "이상 항목을 골라준다"는 목적 자체를 무력화합니다.

**부수 효과로 성능도 지배합니다.** SELECT 비율이 100%라 detail 290만 행을 만들고
payload가 825 MB가 됩니다. 임계를 고치면 이게 5~10%로 떨어져 **전체 시간이 절반 이하**가 됩니다.

---

## 무엇으로 바꾸는가 — Grubbs 검정

표본 크기를 임계에 반영하는 표준 이상치 검정입니다.

```
G_crit(n, α) = (n-1)/√n × √( t² / (n-2+t²) ),   t = t_{α/(2n), n-2}
```

| n | 고정 3 | **Grubbs (α=0.05)** |
|---:|---:|---:|
| 5 | 3.000 | **1.715** |
| 10 | 3.000 | **2.290** |
| 145 | 3.000 | **3.506** |
| 1,000 | 3.000 | **4.040** |
| 3,000 | 3.000 | **4.299** |

소표본에서는 임계가 내려가 판정이 **가능해지고**, 대표본에서는 올라가 위양성이 **억제**됩니다.

**scipy 없이 동작하도록 임계값 표를 미리 계산해 뒀습니다** → 첨부한 `grubbs_table.py`
(4.4 KB, 의존성은 `math` 뿐, n=3~20000 전수 대조 최대오차 0.0007)

---

## 진행 순서

| 단계 | 작업 | 판정 변경 |
|---|---|---|
| **S0** | 브랜치 + **판정 비교 도구** | 없음 |
| **S1** | 통계 코어 단일화 (`stats_core.py`) | 없음 (기본값 동일) |
| **S2** | ddof=1 전환 | **있음** |
| **S3** | Grubbs 임계 도입 | **있음** ← 핵심 |
| **S4** | `diff_ratio` 부호·폭발 수정 | **있음** |
| **S5** | `0.0` 반환 → `None` | **있음** |
| **S6** | 재시험 이력 → `Intermittent` | **있음** |
| **S7** | 스냅샷 재생성 + 성능 재측정 | — |

**한 단계씩 하고 매번 판정 비교 리포트를 보세요.** 여러 개를 한꺼번에 바꾸면
어느 변경이 어떤 영향을 줬는지 알 수 없습니다.

---

## §S0. 판정 비교 도구 — 가장 먼저

회귀 테스트는 "숫자가 안 바뀌었나"를 봅니다. 이 작업은 **숫자를 일부러 바꾸는** 작업이라
회귀 테스트로는 검증이 안 됩니다. 대신 **무엇이 어떻게 바뀌었는지 보여주는 도구**가 필요합니다.

```text
프로젝트 루트의 CLAUDE.md 를 읽고 시작해줘. 여기서부터는 판정 결과를 의도적으로 바꾸는
작업이므로 §5-1 의 "판정 숫자를 바꾸지 말 것" 은 이 브랜치에 한해 적용하지 않는다.
대신 모든 변경은 아래 비교 도구로 영향을 정량 보고해야 한다.

tools/verdict_diff.py 를 만들어줘.

## 목적
코드 변경 전후로 판정이 어떻게 달라졌는지 항목·샘플 단위로 비교해 보여준다.

## 동작
- 입력: --data-root <경로>  --baseline <디렉터리>  [--save <디렉터리>]
- --save 만 주면: 현재 코드의 판정 결과를 baseline 으로 저장한다
- --baseline 을 주면: 현재 코드 결과와 비교해 리포트를 출력한다

## 저장할 것 (조합별)
- 항목 단위: item, n, mean, sigma, mea_s, diff_s, result
- 샘플 단위: item, sample, device_id, mea_s, diff_s, result
- 요약: 전체 항목 수, SELECT 수, OK 수, INSUFFICIENT/NOT EVALUATED 수

## 리포트 출력
1. 요약 비교
     SELECT   999 → 47   (-952)
     OK         0 → 941  (+941)
     판정불가    0 → 11   (+11)
2. 판정 전이 행렬
              →OK  →SELECT  →판정불가
     OK        941      0        0
     SELECT    941     47       11
3. 새로 SELECT 된 항목 상위 20개 (mea_s 내림차순, 이유 포함)
4. SELECT 에서 빠진 항목 상위 20개 (이전 mea_s 와 새 임계값 비교)
5. 수치가 바뀐 항목 상위 20개 (mea_s 변화량 순)

## 주의
- 기존 소스는 이 단계에서 한 줄도 수정하지 마라. 새 파일만 추가한다.
- CLAUDE.md §5-3, §5-4 를 지켜라. 실제 데이터로만 검증하고 우회하지 마라.

## 완료 기준
- python tools/verdict_diff.py --data-root <경로> --save tests/verdict_base/ 가 정상 종료
- 코드를 안 바꾼 상태에서 --baseline tests/verdict_base/ 로 비교하면 "차이 없음" 이 나온다
```

**완료 후 반드시 baseline 을 저장해 두세요.** 이후 모든 단계에서 이걸 기준으로 비교합니다.

```powershell
python tools/verdict_diff.py --data-root "...\Test Data" --save tests/verdict_base/
git add -A && git commit -m "test: 판정 비교 도구 추가 및 기준선 저장"
```

---

## §S1. 통계 코어 단일화 (판정 변경 없음)

```text
프로젝트 루트의 CLAUDE.md 를 읽고 시작해줘.

통계 함수가 두 모듈에 중복 구현되어 이미 동작이 갈라져 있다. 하나로 합쳐줘.
**이 단계에서는 판정 결과가 바뀌면 안 된다.** 기본값을 현재 동작과 똑같이 유지해라.

## 현재 상태
- Pass 모드: cdf_compare_web.py 의 vector_stats (np.std(), ddof=0)
- Fail 모드: cdf_compare_tool.py 의 sample_std (np.std() / statistics.pstdev, 둘 다 ddof=0)
- 이름은 sample_std 인데 실제로는 모표준편차다
- 경계 동작(nan, 빈 배열, n<2)이 두 구현에서 다르다

## 할 일
1. stats_core.py 를 새로 만든다. 의존성은 numpy 와 표준 라이브러리만.

   mean_of(values, ...)          평균
   std_of(values, ddof=0, ...)   표준편차 — **기본값 ddof=0 (현재 동작)**
   zscore(values, center, spread)
   max_abs_z(values, center, spread)
   diff_ratio(pre, post)         현재 구현 그대로
   flag_result(mea_s, diff_s, limit=3.0)   현재 규칙 그대로

   모든 함수에 다음을 명시적으로 문서화해라.
   - 빈 입력일 때 무엇을 반환하는가
   - n < 2 일 때 무엇을 반환하는가
   - nan / inf 를 어떻게 다루는가
   - numpy 유무에 따라 결과가 달라지는가 (달라지면 안 된다)

2. cdf_compare_web.py 와 cdf_compare_tool.py 가 stats_core 를 import 해서 쓰도록 바꾼다.
   중복 구현은 삭제한다.

3. 단위 테스트를 만든다: tests/test_stats_core.py
   - 빈 배열, n=1, n=2, 전부 동일값(σ=0), nan 포함, inf 포함
   - numpy 있을 때와 없을 때(monkeypatch 로 np=None) 결과가 동일한지

## 완료 기준
- regression_check PASS  ← 이 단계는 숫자가 바뀌면 실패다
- verdict_diff 로 baseline 과 비교했을 때 "차이 없음"
- 단위 테스트 전부 통과
```

---

## §S2. ddof=1 전환 (판정 변경 시작)

```text
프로젝트 루트의 CLAUDE.md 를 읽고 시작해줘.

표준편차를 모표준편차(ddof=0)에서 표본표준편차(ddof=1)로 바꿔줘.

## 이유
신뢰성 시험 데이터는 모집단 전수가 아니라 표본이다. 그런데 현재 구현은 ÷n 을 쓴다.
그래서 Excel STDEV, JMP, Minitab 값과 √(n/(n−1)) 배 차이가 난다.
n=5 면 11.8%, n=10 이면 5.4%, n=30 이면 1.7% 작게 나온다.
엔지니어가 다른 툴로 재현하면 반드시 숫자가 안 맞는다.

## 할 일
1. stats_core.std_of 의 기본값을 ddof=1 로 바꾼다.
2. 호출부 전체가 새 기본값을 쓰는지 확인한다. ddof=0 을 명시적으로 넘기는 곳이 남아 있으면
   왜 그런지 설명하거나 제거해라.
3. UI 컬럼 헤더에 정의를 명시한다: "Pre Sigma" → "Pre Sigma (n-1)" 같은 식으로.
   (HTML 문자열 수정을 이 항목에 한해 허용한다)
4. NOTES.md 의 sample_std 항목을 완료로 옮긴다.

## 완료 기준
- verdict_diff 리포트를 출력하고, 아래를 확인해 보고해라.
  * σ 가 모든 항목에서 √(n/(n−1)) 배 커졌는가
  * mea_s 가 그만큼 작아졌는가
  * 판정이 SELECT→OK 로 바뀐 항목 수 (경계 근처만 바뀌어야 정상)
- regression_check 는 **실패가 정상**이다. 실패 내용이 위 변화와 일치하는지만 확인해라.
- 커밋 메시지에 실측 변화량을 넣어라.
```

---

## §S3. Grubbs 임계 도입 ← 핵심 단계

```text
프로젝트 루트의 CLAUDE.md 를 읽고 시작해줘.

고정 3σ 임계를 표본 크기 기반 Grubbs 임계로 바꿔줘.

## 이유 (실측)
Mea_S 는 자기 표본으로 표준화한 z-score 인데, 표본 내 최대 |z| 에는 상한이 있다.
ddof=1 이면 (n-1)/√n, ddof=0 이면 √(n-1) 이다. 그래서 임계 3 은:

     n=5   최대|z| 2.00  → 어떤 이상치도 flag 불가
     n=10  최대|z| 3.00  → flag 불가 (등호라 탈락)
     n=145 순수 노이즈 위양성률 33.2%
     n=1000 위양성률 95.0%
     n=3000 위양성률 99.8%

즉 소로트에서는 침묵하고 대로트에서는 전부 flag 한다.
실제로 순수 정규분포 난수 1,000항목 중 999개가 SELECT 로 나왔다.

## 할 일

1. grubbs_table.py 를 프로젝트에 추가한다 (이미 전달받았다).
   scipy 없이 동작하는 사전 계산 임계값 표다. 의존성은 math 뿐이다.
   내용을 검토하되 값은 바꾸지 마라.

2. stats_core 에 판정 함수를 추가한다.

   def flag_result(mea_s, diff_s, n, alpha=0.05, ddof=1, mode="grubbs", fixed_limit=3.0):
       mode == "fixed"  → 기존 동작 (|z| > fixed_limit)
       mode == "grubbs" → 임계 = grubbs_critical(n, alpha)

       판정 불가 조건을 명시적으로 반환한다:
       - n < 3                          → "INSUFFICIENT N"
       - grubbs_critical(n) 이 None     → "INSUFFICIENT N"
       - σ 가 0 이거나 정의 불가         → "NOT EVALUATED"

3. 기본 모드를 "grubbs" 로 한다. 다만 mode 와 alpha 를 **설정으로 노출**해라.
   - 환경변수 CDFTOOL_FLAG_MODE (fixed|grubbs), CDFTOOL_FLAG_ALPHA (0.05|0.01)
   - 나중에 UI 설정으로 옮기기 쉽게 상수 한 곳에 모아라

4. payload 와 UI 에 임계값을 노출한다. 사용자가 "왜 이게 SELECT 인가"를 알 수 있어야 한다.
   - 요약 행에 threshold 컬럼 추가 (해당 항목의 n 에서 쓰인 임계값)
   - INSUFFICIENT N / NOT EVALUATED 는 회색 태그로 구분 표시
   (HTML 문자열 수정을 이 항목에 한해 허용한다)

5. Fail 모드의 fail_type_for_detail 에도 같은 임계를 적용한다.
   현재 Slight 판정이 |Mea_S|>3 and |Diff_S|>3 으로 하드코딩되어 있다.

## 완료 기준
- verdict_diff 리포트에서 아래를 숫자로 보고해라.
  * Test Data (n=145): SELECT 가 295 → 몇 개가 되는가
  * Perf Data (n=3000): SELECT 가 999 → 몇 개가 되는가 (기대: 한 자리~두 자리)
  * INSUFFICIENT N / NOT EVALUATED 로 분류된 항목 수
- 임계 모드를 fixed 로 되돌리면 (CDFTOOL_FLAG_MODE=fixed) baseline 과 일치하는가
  ← 이게 통과해야 구현이 옳다
- Perf Data 로 bench.py 를 돌려 payload 크기와 전체 시간이 얼마나 줄었는지 보고해라
```

**이 단계가 끝나면 성능도 같이 좋아집니다.** SELECT가 줄면 detail 생성·직렬화·캐시 쓰기가
비례해서 줄어듭니다. 벤치를 꼭 다시 재보세요.

---

## §S4. `diff_ratio` 부호·폭발 수정

```text
프로젝트 루트의 CLAUDE.md 를 읽고 시작해줘.

diff_ratio 의 두 가지 문제를 고쳐줘.

## 문제 1 — pre 가 음수면 부호가 뒤집힌다
현재: return safe_ratio(post_value, pre_value) - 1.0    # 분모가 부호 있는 pre

  pre=-1.0 → post=-1.2 (절댓값 20% 악화)  →  +0.2  (개선처럼 보임)
  pre=-1.0 → post=-0.8 (개선)             →  -0.2  (악화처럼 보임)

오프셋 전압, 온도계수, 음극성 leakage 처럼 음수를 갖는 항목에서 실제로 발생한다.
부호가 반대인 항목이 한 표에 섞이면 Diff 컬럼을 신뢰할 수 없다.

## 문제 2 — pre ≈ 0 에서 폭발한다
현재 pre == 0 만 막는다. pre=1e-12, post=2e-12 면 diff_ratio = 1.0 (100% 변화)다.
절대 변화량은 1 pA 인데 100% 로 보고된다. leakage 계열에서 상시 발생한다.
이 폭발값이 diff_sigma 를 부풀려서 **정상 샘플의 Diff_S 를 전부 0 근처로 눌러**
진짜 이상치를 가린다 (masking).

## 할 일
1. 분모를 abs(pre) 로 바꾸고 변화 방향은 copysign 으로 보존한다.
2. 상대 임계를 도입해 pre 가 0 에 가까우면 None 을 반환한다.
   기준값은 해당 항목 pre 값들의 중앙 절댓값(median |pre|) 을 쓰는 게 안전하다.
   상수 EPS_REL 은 설정으로 노출해라 (기본 1e-6 수준에서 실측으로 조정).
3. **절대 shift (post − pre) 를 payload 와 UI 에 병기한다.**
   비율이 정의되지 않는 항목도 변화량은 볼 수 있어야 한다.
   (HTML 문자열 수정 허용)
4. Diff 가 None 인 샘플은 Diff_S 계산에서 제외하되, 상세 표에는 "N/A" 로 표시한다.

## 완료 기준
- verdict_diff 로 Diff/Diff_S 가 바뀐 항목 수와 그중 SELECT 판정이 뒤집힌 수 보고
- 음수 pre 를 갖는 항목을 하나 골라 부호가 의도대로 나오는지 수동 확인
- 단위 테스트 추가: pre 음수, pre≈0, pre=0, post=0, 양쪽 nan
```

---

## §S5. `0.0` 반환을 `None` 으로

```text
프로젝트 루트의 CLAUDE.md 를 읽고 시작해줘.

"정의 불가"를 "정상"으로 둔갑시키는 반환값들을 고쳐줘.

## 문제
safe_ratio, sigma, mean, sample_std 가 아래 상황에서 0.0 을 반환한다.
- 분모가 0 (σ=0, 즉 모든 샘플이 동일값)
- 빈 데이터
- n < 2

0.0 은 "완벽히 평균값"이라는 뜻이라 판정에서 OK 로 통과한다.
특히 **σ=0 인 항목은 어떤 이상치가 있어도 절대 flag 되지 않는다.**
측정 분해능 한계로 값이 전부 같게 나오는 파라미터(디지털 판정, 카운터)에서 실제로 발생한다.

## 할 일
1. stats_core 의 해당 함수들이 None 을 반환하게 한다.
2. 호출부에서 None 을 "NOT EVALUATED" 로 처리한다. 0 으로 대체하지 마라.
3. UI 에서 N/A 로 표시하고, 요약 통계에서 "판정 불가 N건" 을 별도 카운트한다.
   (HTML 문자열 수정 허용)

## 완료 기준
- verdict_diff 로 NOT EVALUATED 로 분류된 항목 수와 그 사유 분포 보고
  (σ=0 / 데이터 없음 / n<2 각각 몇 건인지)
- σ=0 인 항목이 실제 데이터에 몇 개 있는지 확인해 보고
```

---

## §S6. 재시험 이력 → `Intermittent`

`NOTES.md` P0 항목입니다. 사용자 요구사항은 이미 확정되어 있습니다.

```text
프로젝트 루트의 CLAUDE.md §3-4 와 NOTES.md 의 P0 재시험 항목을 읽고 시작해줘.

같은 파일 안의 재시험 이력을 보존해서 Intermittent 판정이 가능하게 해줘.

## 확정된 요구사항
1. 대표 측정값 = 마지막 회차 (현재 동작 유지 — 바꾸지 마라)
2. 1회차 fail → 마지막 회차 Bin1 로 회복한 유닛 = Intermittent, 벤치 대상 제외

## 할 일
같은 파일 안에서 Serial # 기준으로 등장 순서를 보존한 회차 이력을 만든다.

    history[sample] = [{"bin": ..., "items": {...}, "spec_fail": ...}, ...]
    대표값          = history[sample][-1]
    bin_sequence    = [h["bin"] for h in history[sample]]
    is_intermittent = (not bin_is_pass(bin_sequence[0])) and bin_is_pass(bin_sequence[-1])

파일 기반 stage 병합(merged_fail_rows)은 다른 사업장 데이터 대비 그대로 두고,
그 위에 "파일 내 재시험" 단계를 하나 더 얹어라.

Bin 출처도 통일해라. 현재 Bin 은 첫 회차, 측정값은 마지막 회차라 출처가 어긋난다.
어느 쪽으로 통일할지 코드에 주석으로 명시해라.

## 검증 데이터 문제
현재 Test Data 에는 회복 케이스가 0건이다 (22개 fail 유닛 전부 4회차까지 fail 유지).
회복 케이스가 있는 Post 파일이 없으면 이 기능을 검증할 수 없다.
따라서 tools/make_perf_fixture.py 를 확장해 회복 케이스를 포함한 픽스처를 만들거나,
사용자에게 실제 회복 케이스 파일을 요청해라.

## 완료 기준
- 회복 유닛이 Intermittent 로 분류되고 Need Bench 대상에서 빠진다
- 회복하지 않은 유닛은 기존과 동일하게 Excessive/Slight/Tail 로 분류된다
- verdict_diff 로 fail_type 분포 변화 보고
```

---

## §S7. 마무리 — 스냅샷 재생성 + 성능 재측정

```text
통계 개선이 끝났으니 기준선을 새로 잡자.

1. tests/golden/ 을 지우고 현재 코드 기준으로 회귀 스냅샷을 새로 만들어라.
   (판정이 바뀌었으므로 예전 스냅샷은 무효다)
2. tests/verdict_base/ 도 현재 기준으로 갱신해라.
3. Test Data 와 Perf Data 양쪽으로 bench.py 를 돌려 성능을 재측정해라.
   임계 변경으로 SELECT 가 줄었으니 payload / json.dumps / cache write 가
   같이 줄었을 것이다. 실측치를 보고해라.
4. NOTES.md 에서 완료된 항목을 "완료 / 결정됨" 으로 옮기고,
   각 항목에 실측 변화량을 한 줄로 남겨라.
5. CLAUDE.md §1 의 판정 로직 설명과 §6 의 알려진 문제 표를 현재 상태로 갱신해라.

## 완료 기준
- regression_check PASS (새 스냅샷 기준)
- verdict_diff 가 "차이 없음"
- 성능 실측치 표 (이전 / 이후)
```

---

## 손대지 말 것 (이 브랜치에서도)

| 항목 | 이유 |
|---|---|
| Pre↔Post 조인 키 (DEVICE_ID) | 이미 확정. 건드리면 매칭이 깨진다 |
| 항목 탐지 규칙 (`find_test_item_name_row` 등) | 파싱이 깨진다 |
| `Mea_S` / `Diff_S` 의 OR 조건 | 도구의 핵심 설계 (`CLAUDE.md` §1) |
| 화면 표시용 Sample No. (Serial #) | 조인만 DEVICE_ID, 표시는 Serial # |
| 파서 강건성 (`NOTES.md` P1) | 별도 작업 |
| CSRF / 보안 (`NOTES.md` P2) | 별도 작업 |

---

## 나중에 고려할 것 (이번 범위 밖)

- **FDR 보정** — Grubbs 는 항목 하나 안에서의 다중비교만 다룬다. 항목이 1,000개면
  항목 간 다중비교도 있다. Benjamini–Hochberg 를 얹으면 더 정확해진다.
- **robust z (MAD)** — 이상치가 평균·σ를 부풀려 자기 자신을 가리는 masking 을 막는다.
  `0.6745 × (x − median) / MAD`
- **GESD** — 한 항목에 이상치가 여러 개일 때 Grubbs 보다 낫다.
- **플로팅 포지션** `i/n` → median rank `(i−0.3)/(n+0.4)` — 확률지·와이블 적합의 전제
- **리드아웃 추세를 판정에 반영** — T1/T2/T3 단조 드리프트는 가장 강한 열화 신호인데
  현재 판정은 최종 리드아웃 하나만 본다 (payload 에 데이터는 이미 있다)
